# Backend Configuration (for Docker)
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/projecttasks
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=your_db_password
SPRING_JPA_HIBERNATE_DDL_AUTO=update
SPRING_JPA_SHOW_SQL=true

# PostgreSQL Configuration
POSTGRES_DB=projecttasks
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_postgres_password

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_min_32_chars
JWT_EXPIRATION=86400000

# Frontend Configuration
# Base API URL (optional – leave empty to use default behavior)
# Local development: http://localhost:8080
# Docker production: leave empty (Nginx reverse proxy)
VITE_API_BASE_URL=
